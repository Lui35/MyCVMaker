# Create a local .env file and add your Groq key. Never commit the real value.
GROQ_API_KEY=replace_with_your_groq_key

# Strict structured outputs are supported by this model.
GROQ_MODEL=openai/gpt-oss-20b

# Optional Docker Compose host ports.
WEB_PORT=5173
API_PORT=8000
